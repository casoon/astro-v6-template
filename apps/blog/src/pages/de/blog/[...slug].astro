---
import BlogPostPage from '@/components/BlogPostPage.astro';
import { getBlogPostStaticPaths } from '@/utils/blog-posts';
import '@/styles/app.css';

export const getStaticPaths = getBlogPostStaticPaths;

const { post } = Astro.props;
---

<BlogPostPage locale="de" post={post} />
